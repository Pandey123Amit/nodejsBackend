version: "3.9"

services:
  # rms:
  #   build: ./
  #   container_name: rms
  #   ports:
  #     - "5000:3000"
    # env_file:
    #   - .env   

  postgres:
    image: postgis/postgis:16-3.4
    container_name: postgres_db
    restart: always
    env_file:
      - .env   
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: {}


